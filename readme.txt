To work in this application, you need to run it and open a browser at the address "localhost:8080/"

All accounts are initialized in the class "com.divanxan.server.HttpServer" in fillingUpUsers() method.

To create a transaction in the browser, type the id of two accounts and the amount debited.